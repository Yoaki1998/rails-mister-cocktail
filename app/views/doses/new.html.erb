<h1>New Dose</h1>

<%= simple_form_for([@cocktail, @dose]) do |f| %>
  <%= f.input :description %>
  <%= f.association :ingredient %>
  <%= f.button :submit %>
<% end %>
<p class="btn btn-warning unlink"> <%= link_to "Back", cocktail_path(@cocktail) %> </p>
